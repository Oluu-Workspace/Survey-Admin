# Legacy admin pages (mock data)

These screens were generated early in the Lovable prototype. They are **not routed** in `App.tsx` and are kept for reference only.

Use instead:

- **Data Explorer** — `/dashboard/data`
- **Review** — `/dashboard/review`
- **Projects** — `/dashboard/projects`
- **Surveys / Agents** — `/dashboard/surveys`, `/dashboard/agents`
