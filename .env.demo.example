# Survey-Admin (research dashboard) — Vercel Environment Variables
# Project → Settings → Environment Variables → add for Production (+ Preview if you want)

# Required (no trailing slash after v1)
VITE_API_BASE_URL=https://survey-backend.project360.space/api/v1

# Local dev: copy to .env.local
# VITE_API_BASE_URL=http://localhost:5080/api/v1
